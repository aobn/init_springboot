# 数据库命名规则文档

## 目录
- [1. 概述](#1-概述)
- [2. 基本命名原则](#2-基本命名原则)
- [3. 数据库表命名规范](#3-数据库表命名规范)
- [4. 数据库字段命名规范](#4-数据库字段命名规范)
- [5. 字段类型映射规范](#5-字段类型映射规范)
- [6. 索引命名规范](#6-索引命名规范)
- [7. 与Java/API的映射关系](#7-与javaapi的映射关系)
- [8. 最佳实践](#8-最佳实践)

## 1. 概述

### 1.1 文档目标
本文档定义了数据库设计中的命名标准，包括表名、字段名、索引名等命名规范，确保数据库结构的一致性、可维护性和团队协作效率。

### 1.2 设计原则
- **统一性**: 所有数据库对象使用统一的命名风格
- **可读性**: 命名应清晰表达其业务含义和用途
- **简洁性**: 在保证清晰的前提下尽量简洁
- **标准化**: 遵循MySQL数据库命名最佳实践
- **可扩展**: 命名规范应支持项目的长期发展

### 1.3 适用范围
- MySQL 5.7及以上版本
- InnoDB存储引擎
- UTF8MB4字符集
- Spring Boot + MyBatis项目

## 2. 基本命名原则

### 2.1 通用原则
- **命名风格**: 统一使用下划线命名法（snake_case）
- **字符集**: 仅使用小写字母、数字和下划线
- **长度限制**: 名称长度建议3-64个字符（MySQL限制）
- **语义性**: 名称应具有明确的业务含义
- **英文命名**: 禁止使用拼音或中英文混合

### 2.2 禁用命名
- **拼音命名**: `yonghu`、`dingdan`
- **中英文混合**: `user表`、`订单_order`
- **大写字母**: `UserTable`、`ORDER_ID`
- **特殊字符**: `user-name`、`user@table`
- **MySQL关键字**: `order`、`group`、`index`（需要时加前缀）
- **数字开头**: `1st_table`、`2nd_field`

### 2.3 推荐命名
- **下划线分隔**: `user_info`、`order_detail`
- **语义明确**: `create_time`、`user_status`
- **标准后缀**: `_id`、`_time`、`_flag`
- **业务前缀**: `sys_config`、`biz_order`

## 3. 数据库表命名规范

### 3.1 表命名基本规则
- **命名风格**: 使用下划线命名法（snake_case）
- **单复数**: 统一使用单数形式
- **长度**: 建议3-30个字符
- **前缀**: 可选择性使用业务前缀

### 3.2 表命名分类标准

| 表类型 | 命名规范 | 示例 | 说明 |
|--------|----------|------|------|
| **业务主表** | 单数形式 | `user`, `order`, `product` | 核心业务实体表 |
| **关联表** | 表名_表名 | `user_role`, `order_item`, `product_category` | 多对多关联表 |
| **配置表** | 以config结尾 | `system_config`, `email_config`, `pay_config` | 系统配置表 |
| **日志表** | 以log结尾 | `operation_log`, `login_log`, `error_log` | 日志记录表 |
| **临时表** | temp_开头 | `temp_import`, `temp_export`, `temp_calculation` | 临时数据表 |
| **历史表** | 原表名_history | `user_history`, `order_history` | 历史数据表 |
| **备份表** | 原表名_backup | `user_backup`, `order_backup` | 数据备份表 |
| **统计表** | 以stat结尾 | `user_stat`, `order_stat`, `daily_stat` | 统计汇总表 |

### 3.3 业务模块表命名

| 业务模块 | 表名示例 | 说明 |
|----------|----------|------|
| **用户模块** | `user`, `user_profile`, `user_role` | 用户相关表 |
| **认证模块** | `auth_token`, `auth_log`, `verification_code` | 认证相关表 |
| **订单模块** | `order`, `order_item`, `order_payment` | 订单相关表 |
| **商品模块** | `product`, `product_category`, `product_image` | 商品相关表 |
| **系统模块** | `system_config`, `system_log`, `system_menu` | 系统相关表 |
| **文件模块** | `file_info`, `file_upload`, `file_category` | 文件相关表 |

## 4. 数据库字段命名规范

### 4.1 字段命名基本规则
- **命名风格**: 统一使用下划线命名法（snake_case）
- **字符集**: 仅使用小写字母、数字和下划线
- **长度**: 字段名长度建议3-64个字符
- **语义性**: 字段名应具有明确的业务含义

### 4.2 字段命名分类标准

| 字段类型 | 命名规范 | 示例 | 说明 |
|----------|----------|------|------|
| **主键ID** | id | `id` | 主键统一使用id |
| **外键ID** | 关联表名_id | `user_id`, `order_id`, `category_id` | 外键关联字段 |
| **时间字段** | 以_time结尾 | `create_time`, `update_time`, `delete_time` | 时间戳字段 |
| **日期字段** | 以_date结尾 | `birth_date`, `expire_date`, `start_date` | 日期字段 |
| **状态字段** | 以_status结尾 | `user_status`, `order_status`, `pay_status` | 状态枚举值 |
| **类型字段** | 以_type结尾 | `user_type`, `file_type`, `message_type` | 类型枚举值 |
| **标志字段** | is_开头或_flag结尾 | `is_active`, `is_deleted`, `delete_flag` | 布尔值标志 |
| **数量字段** | 以_count/_num结尾 | `total_count`, `page_num`, `retry_count` | 数值统计 |
| **金额字段** | 以_amount/_price结尾 | `total_amount`, `unit_price`, `discount_amount` | 金额相关 |
| **比例字段** | 以_rate/_ratio结尾 | `tax_rate`, `discount_ratio`, `success_rate` | 比例百分比 |
| **排序字段** | 以_order/_sort结尾 | `sort_order`, `display_sort`, `menu_order` | 排序序号 |
| **索引字段** | 以_index结尾 | `sort_index`, `display_index` | 排序索引 |
| **编码字段** | 以_code结尾 | `user_code`, `product_code`, `error_code` | 编码标识 |
| **名称字段** | 以_name结尾 | `user_name`, `file_name`, `category_name` | 名称字段 |
| **描述字段** | 以_desc/_description结尾 | `product_desc`, `error_description` | 描述信息 |
| **地址字段** | 以_address/_addr结尾 | `email_address`, `ip_addr`, `home_address` | 地址信息 |
| **URL字段** | 以_url结尾 | `avatar_url`, `callback_url`, `redirect_url` | URL链接 |
| **路径字段** | 以_path结尾 | `file_path`, `image_path`, `upload_path` | 文件路径 |

### 4.3 常用业务字段规范

| 业务场景 | 数据库字段名 | 字段类型 | 长度 | 说明 |
|----------|--------------|----------|------|------|
| **用户信息** | `username` | VARCHAR | 50 | 用户名（登录名） |
| **用户信息** | `password` | VARCHAR | 100 | 密码（加密后） |
| **用户信息** | `email` | VARCHAR | 100 | 邮箱地址 |
| **用户信息** | `phone` | VARCHAR | 20 | 手机号码 |
| **用户信息** | `real_name` | VARCHAR | 50 | 真实姓名 |
| **用户信息** | `nick_name` | VARCHAR | 50 | 昵称 |
| **用户信息** | `avatar_url` | VARCHAR | 255 | 头像地址 |
| **用户信息** | `gender` | TINYINT | - | 性别(0:未知,1:男,2:女) |
| **用户信息** | `birth_date` | DATE | - | 出生日期 |
| **用户信息** | `user_role` | VARCHAR | 20 | 用户角色 |
| **状态字段** | `user_status` | TINYINT | - | 用户状态(0:禁用,1:启用) |
| **状态字段** | `account_status` | TINYINT | - | 账户状态(0:正常,1:锁定,2:冻结) |
| **标志字段** | `is_deleted` | TINYINT | - | 删除标志(0:未删除,1:已删除) |
| **标志字段** | `is_active` | TINYINT | - | 激活状态(0:未激活,1:已激活) |
| **标志字段** | `is_verified` | TINYINT | - | 验证状态(0:未验证,1:已验证) |
| **时间字段** | `create_time` | TIMESTAMP | - | 创建时间 |
| **时间字段** | `update_time` | TIMESTAMP | - | 更新时间 |
| **时间字段** | `delete_time` | TIMESTAMP | - | 删除时间 |
| **时间字段** | `login_time` | TIMESTAMP | - | 最后登录时间 |
| **时间字段** | `expire_time` | TIMESTAMP | - | 过期时间 |
| **版本控制** | `version` | INT | - | 乐观锁版本号 |
| **排序字段** | `sort_order` | INT | - | 排序序号 |
| **创建信息** | `created_by` | BIGINT | - | 创建人ID |
| **更新信息** | `updated_by` | BIGINT | - | 更新人ID |
| **备注信息** | `remark` | TEXT | - | 备注说明 |
| **扩展字段** | `extra_data` | JSON | - | 扩展数据（JSON格式） |

### 4.4 字段命名禁用示例

| ❌ 错误命名 | ✅ 正确命名 | 说明 |
|-------------|-------------|------|
| `UserName` | `username` | 字段名应该全小写 |
| `createTime` | `create_time` | 应该使用下划线分隔 |
| `isDelete` | `is_deleted` | 布尔字段应该语义完整 |
| `userRole` | `user_role` | 应该使用下划线分隔 |
| `phoneNumber` | `phone` | 避免冗余的后缀 |
| `emailAddress` | `email` | 简化常用字段名 |
| `yonghuming` | `username` | 禁止使用拼音 |
| `user-name` | `username` | 不能使用连字符 |
| `1st_name` | `first_name` | 不能数字开头 |
| `user@name` | `username` | 不能包含特殊字符 |

## 5. 字段类型映射规范

### 5.1 数据类型选择标准

| 数据类型 | 适用场景 | 长度建议 | Java类型 | 说明 |
|----------|----------|----------|----------|------|
| **BIGINT** | 主键ID、大整数 | - | Long | 自增主键、外键ID |
| **INT** | 普通整数 | - | Integer | 年龄、数量、状态码 |
| **TINYINT** | 小整数、布尔值 | - | Integer/Boolean | 状态标志、性别 |
| **DECIMAL** | 精确小数 | (10,2) | BigDecimal | 金额、价格 |
| **FLOAT/DOUBLE** | 近似小数 | - | Float/Double | 比例、评分 |
| **VARCHAR** | 变长字符串 | 根据需要 | String | 用户名、邮箱、描述 |
| **CHAR** | 定长字符串 | 根据需要 | String | 固定长度编码 |
| **TEXT** | 长文本 | - | String | 文章内容、详细描述 |
| **TIMESTAMP** | 时间戳 | - | LocalDateTime | 创建时间、更新时间 |
| **DATE** | 日期 | - | LocalDate | 出生日期、到期日期 |
| **TIME** | 时间 | - | LocalTime | 营业时间、提醒时间 |
| **JSON** | JSON数据 | - | String/Object | 扩展属性、配置信息 |
| **BLOB** | 二进制数据 | - | byte[] | 文件内容、图片 |

### 5.2 字段长度建议

| 字段类型 | 建议长度 | 示例字段 | 说明 |
|----------|----------|----------|------|
| **用户名** | VARCHAR(50) | `username` | 支持中英文用户名 |
| **密码** | VARCHAR(100) | `password` | 加密后的密码 |
| **邮箱** | VARCHAR(100) | `email` | 标准邮箱地址 |
| **手机号** | VARCHAR(20) | `phone` | 支持国际手机号 |
| **姓名** | VARCHAR(50) | `real_name` | 支持中英文姓名 |
| **地址** | VARCHAR(255) | `address` | 详细地址信息 |
| **URL** | VARCHAR(255) | `avatar_url` | 文件链接地址 |
| **编码** | VARCHAR(32) | `user_code` | 业务编码 |
| **标题** | VARCHAR(200) | `title` | 文章标题、商品名称 |
| **描述** | TEXT | `description` | 详细描述信息 |
| **备注** | TEXT | `remark` | 备注说明 |

### 5.3 字段属性设置

| 属性 | 设置规范 | 示例 | 说明 |
|------|----------|------|------|
| **NOT NULL** | 必填字段设置 | `username VARCHAR(50) NOT NULL` | 防止空值 |
| **DEFAULT** | 设置默认值 | `status TINYINT DEFAULT 1` | 提供默认值 |
| **AUTO_INCREMENT** | 主键自增 | `id BIGINT AUTO_INCREMENT` | 主键自动递增 |
| **COMMENT** | 字段注释 | `COMMENT '用户名'` | 必须添加注释 |
| **CHARSET** | 字符集 | `CHARSET utf8mb4` | 支持emoji |
| **COLLATE** | 排序规则 | `COLLATE utf8mb4_unicode_ci` | 不区分大小写 |

## 6. 索引命名规范

### 6.1 索引命名规则

| 索引类型 | 命名规范 | 示例 | 说明 |
|----------|----------|------|------|
| **主键索引** | PRIMARY | `PRIMARY KEY (id)` | 系统自动命名 |
| **唯一索引** | uk_字段名 | `uk_username`, `uk_email` | unique key缩写 |
| **普通索引** | idx_字段名 | `idx_create_time`, `idx_status` | index缩写 |
| **复合索引** | idx_字段1_字段2 | `idx_user_id_status`, `idx_create_time_type` | 多字段组合 |
| **外键索引** | fk_表名_字段 | `fk_user_role_id`, `fk_order_user_id` | foreign key缩写 |
| **全文索引** | ft_字段名 | `ft_title`, `ft_content` | fulltext缩写 |

## 7. 与Java/API的映射关系

### 7.1 命名转换规则

| 层级 | 命名规范 | 示例 | 转换规则 |
|------|----------|------|----------|
| **数据库字段** | snake_case | `create_time`, `user_status` | 下划线分隔，全小写 |
| **Java属性** | camelCase | `createTime`, `userStatus` | 驼峰命名，首字母小写 |
| **JSON参数** | camelCase | `createTime`, `userStatus` | 与Java属性保持一致 |


### 7.2 映射示例对照表

| 数据库字段 | Java属性 | JSON参数 | 说明 |
|------------|----------|----------|------|
| `user_id` | `userId` | `userId` | 用户ID |
| `username` | `username` | `username` | 用户名 |
| `create_time` | `createTime` | `createTime` | 创建时间 |
| `update_time` | `updateTime` | `updateTime` | 更新时间 |
| `is_deleted` | `isDeleted` | `isDeleted` | 删除标志 |
| `user_status` | `userStatus` | `userStatus` | 用户状态 |
| `real_name` | `realName` | `realName` | 真实姓名 |
| `avatar_url` | `avatarUrl` | `avatarUrl` | 头像地址 |
| `birth_date` | `birthDate` | `birthDate` | 出生日期 |
| `total_amount` | `totalAmount` | `totalAmount` | 总金额 |

## 8. 最佳实践

### 8.1 检查清单

在创建数据库对象时，请确认以下规范：

- [ ] 表名使用下划线命名法，全小写
- [ ] 字段名使用下划线命名法，语义清晰
- [ ] 主键统一命名为id，外键使用表名_id格式
- [ ] 时间字段统一使用_time后缀
- [ ] 状态字段统一使用_status后缀
- [ ] 标志字段使用is_开头或_flag结尾
- [ ] 所有字段都添加了COMMENT注释
- [ ] 索引命名遵循uk_/idx_/fk_前缀规范
- [ ] 字段类型和长度设置合理
- [ ] 避免使用MySQL关键字作为字段名

---

**文档版本**: v1.0.0  
**维护团队**: 数据库设计组  
**联系方式**: db-team@example.com  
**参考标准**: MySQL 8.0 官方文档